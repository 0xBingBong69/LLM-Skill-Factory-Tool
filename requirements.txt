# LLM Skill Factory — runtime dependencies

# --- Web API (primary UI: FastAPI backend + React frontend in web/) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # multipart uploads for reference extraction
pydantic>=2.0

# --- Core ---
openai>=1.40          # used only as an OpenAI-compatible client pointed at OpenRouter
python-dotenv>=1.0
PyYAML>=6.0

# Optional: enables PDF reference-material extraction. The app degrades gracefully if absent.
pypdf>=4.0

# --- Legacy Streamlit UI (kept available via `streamlit run app.py`) ---
streamlit>=1.36

# Dev / testing
pytest>=8.0
httpx>=0.27           # FastAPI TestClient
